org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.configuration-cache=true
android.useAndroidX=true
kotlin.code.style=official

# The Verbum backend for debug builds (api/openapi.yaml). Emulator route to this machine is
# http://10.0.2.2:8080; a backend on another box on the LAN is its address. Release ignores this.
VERBUM_API_BASE_URL=https://api.vendlydigital.com.br
