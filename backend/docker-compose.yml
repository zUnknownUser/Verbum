# Local Postgres for the content store. `docker compose up -d`
# then `export VERBUM_DATABASE_URL=postgres://verbum:verbum@localhost:5432/verbum?sslmode=disable`.
services:
  db:
    image: pgvector/pgvector:pg17 # postgres:17 plus the pgvector extension (§27-29 hybrid search)
    environment:
      POSTGRES_USER: verbum
      POSTGRES_PASSWORD: verbum
      POSTGRES_DB: verbum
    ports: ["5432:5432"]
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/migrations:/docker-entrypoint-initdb.d:ro   # applied once, on first start
volumes:
  pgdata:
